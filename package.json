{
  "name": "fuel-and-train",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "hello": "node --env-file=.env --import tsx ./src/client/hello.ts",
    "hello-stream": "node --env-file=.env --import tsx ./src/client/hello-stream.ts",
    "intake": "node --env-file=.env --import tsx ./src/intake/intake.ts",
    "tools": "node --env-file=.env --import tsx src/tools/chat.ts",
    "agent": "node --env-file=.env --import tsx src/agent/agent.ts",
    "batch": "node --env-file=.env --import tsx src/batch/batch-cli.ts",
    "batch:dry": "node --env-file=.env --import tsx src/batch/batch-cli.ts --dry-run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.7.0",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@vitest/coverage-v8": "4.1.10",
    "dotenv": "^17.4.2",
    "tsx": "^4.23.7",
    "vite": "^8.2.2",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0"
  }
}